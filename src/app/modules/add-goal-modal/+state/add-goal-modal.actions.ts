import { createAction } from '@ngrx/store';

export const showAddGoalModal = createAction('[AddGoalModal] SHOW ADD GOAL');
export const hideAddGoalModal = createAction('[AddGoalModal] HIDE ADD GOAL');